module.exports = {
  // Published under a nested static path (/demos/sidra-noor-fashion/), not at a
  // domain root, so every asset URL has to resolve relative to the page.
  publicPath: './',
  productionSourceMap: false,
};
