const API_BASE = window.VELOURA_API_BASE || (window.location.hostname.includes('localhost') ? 'http://localhost:8787/api' : '/api');
const money = (value) => `৳${Number(value || 0).toLocaleString('en-BD')}`;
const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]));
async function loadInvoice(tokenOverride) { const orderCode = new URLSearchParams(location.search).get('order'); const token = tokenOverride || sessionStorage.getItem('veloura-admin-token') || localStorage.getItem('veloura-customer-token'); const headers = token ? { Authorization: `Bearer ${token}` } : {}; if (!orderCode) return document.querySelector('#invoice-root').innerHTML = '<p class="muted">Order ID is required.</p>'; const response = await fetch(`${API_BASE}/orders/${encodeURIComponent(orderCode)}/invoice`, { headers }); const data = await response.json(); if (!response.ok) return document.querySelector('#invoice-root').innerHTML = `<p class="muted">${escapeHtml(data.error || 'Invoice not available. Please open it from your account or admin dashboard.')}</p>`; const invoice = data.invoice; document.title = `${invoice.invoiceNumber || invoice.orderCode} · Veloura Atelier`; document.querySelector('#invoice-root').innerHTML = `<div class="invoice-head"><div><p class="eyebrow">VELOURA ATELIER</p><h1>Invoice</h1><p class="muted">Beauty, thoughtfully chosen.</p></div><div class="invoice-meta"><strong>${escapeHtml(invoice.invoiceNumber || invoice.orderCode)}</strong><br>${escapeHtml(invoice.createdAt)}<br>Status: ${escapeHtml(invoice.status)}</div></div><div class="bill-grid"><div><h2>Bill to</h2><p><strong>${escapeHtml(invoice.name)}</strong></p><p>${escapeHtml(invoice.phone)}</p><p>${escapeHtml(invoice.email || '')}</p><p>${escapeHtml(invoice.address)}, ${escapeHtml(invoice.upazila)}, ${escapeHtml(invoice.district)}</p></div><div><h2>Order details</h2><p>Payment: ${escapeHtml(invoice.paymentMethod)} (${escapeHtml(invoice.paymentStatus)})</p><p>Order source: ${escapeHtml(invoice.orderSource)}</p><p>Delivery: ${escapeHtml(invoice.deliveryZone)} · ${money(invoice.deliveryFee)}</p><p>Package weight: ${Number(invoice.packageWeightGrams || 0).toLocaleString('en-BD')} g</p></div></div><table><thead><tr><th>Product</th><th>Qty</th><th>Unit price</th><th>Weight</th><th>Line total</th></tr></thead><tbody>${(data.items || []).map((item) => `<tr><td>${escapeHtml(item.productName)}</td><td>${item.quantity}</td><td>${money(item.unitPrice)}</td><td>${Number(item.weightGrams || 0).toLocaleString('en-BD')} g</td><td>${money(item.unitPrice * item.quantity)}</td></tr>`).join('')}</tbody></table><div class="invoice-total"><div><span>Subtotal</span><strong>${money(invoice.subtotal)}</strong></div><div><span>Delivery</span><strong>${money(invoice.deliveryFee)}</strong></div><div class="grand"><span>Total</span><strong>${money(invoice.total)}</strong></div></div>`; }
let invoiceLoaded = false; const showInvoiceError = () => { if (!invoiceLoaded) document.querySelector('#invoice-root').innerHTML = '<p class="muted">Invoice could not be loaded. Please reopen it from the admin dashboard or your account.</p>'; }; const startInvoice = (token) => loadInvoice(token).then(() => { invoiceLoaded = true; }); const params = new URLSearchParams(location.search); const orderCode = params.get('order'); const initialToken = sessionStorage.getItem('veloura-admin-token') || localStorage.getItem('veloura-customer-token'); const channelId = params.get('channel'); if (initialToken) { startInvoice(initialToken).catch(showInvoiceError); } else if (channelId && /^[a-zA-Z0-9-]{10,100}$/.test(channelId) && orderCode) { const channel = 'BroadcastChannel' in window ? new BroadcastChannel(`veloura-invoice-${channelId}`) : null; let readyTimer; let timeout; let cleanup = () => {}; const acceptAuth = (event) => { if (event.data?.type !== 'veloura-invoice-auth' || event.data.orderCode !== orderCode || typeof event.data.token !== 'string' || invoiceLoaded) return; cleanup(); startInvoice(event.data.token).catch(showInvoiceError); }; const sendReady = () => { channel?.postMessage({ type: 'veloura-invoice-ready', orderCode, channelId }); if (window.opener) window.opener.postMessage({ type: 'veloura-invoice-ready', orderCode, channelId }, window.location.origin); }; channel?.addEventListener('message', acceptAuth); window.addEventListener('message', acceptAuth); cleanup = () => { window.clearInterval(readyTimer); window.clearTimeout(timeout); channel?.close(); window.removeEventListener('message', acceptAuth); }; sendReady(); readyTimer = window.setInterval(sendReady, 250); timeout = window.setTimeout(() => { cleanup(); showInvoiceError(); }, 7000); } else { showInvoiceError(); }
